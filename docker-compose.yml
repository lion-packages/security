services:
    app:
        container_name: security-app
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8000:8000"
        volumes:
            - ./:/var/www/html
networks:
    lion:
        driver: bridge
